[project]
name = "kafka-producer"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "confluent-kafka==2.3.0",
    "pyyaml==6.0.1",
    "requests>=2.32.5",
]
